name: solar_icons_plus_example
description: Example app demonstrating the solar_icons_plus package.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  solar_icons_plus:
    path: ../
  flutter_colorpicker: ^1.1.0
  url_launcher: ^6.1.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
